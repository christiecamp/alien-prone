<div class="card">
    <div class="card-header" style="background-color:black;" >
        <h4><a class="link text-light" href="/dashboard/edit/{{id}}">{{title}}</a></h4>
        <h6 class="mb-2 text-light">{{format_date}} {{format_time}} </h6>
    </div>
    <div class="card-body">
        <p class="card-text">{{content}}</p>
    </div>
</div>